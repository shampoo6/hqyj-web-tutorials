# 要求
请声明一个函数 `clamp` (定界函数) , 接收三个参数如下
```js
// x: 任意数字
// min: 最小取值
// max: 最大取值
function clamp(x, min, max){
    // 请补充函数逻辑
}
```

`clamp` 函数的功能是：传入参数 `x`， `x` 如果在 `min` `max` 的范围内，则返回 `x` 本身，若小于 `min` 就返回 `min`，若大于 `max` 就返回 `max`

请使用数学函数完成该功能